# 行业黑话词典更新日志

本文件简要记录词典的每次版本更新，最新版本放在最前面。

## 版本规则

- 根目录 `VERSION` 是当前词典版本的唯一标识。
- 当前版本以根目录 `VERSION` 为准。
- 用户确认并实际提交的一批词典内容修改只增加一次补丁版本，例如 `v1.0 → v1.1 → v1.2`。
- 仅查看、提取、分析、提出建议或等待确认时不增加版本。
- 拼写修正、解释调整、词条增删、置信度变化、脱敏规则变化和文件结构变化，实际提交后均记录在对应版本下。
- 每次更新至少记录日期、词条变化、边界变化和脱敏检查结果。

## v1.1 — 2026-09-12

状态：当前版本。

更新内容：

- 根据用户明确确认，将`冷轧板`直接归入`冷板`的高置信度同义词。
- 删除`冷轧板`原有的“人工确认”独立行，并合并到`冷板`主别称列表。
- 同步更新词典数据结构示例中的别称集合。
- 本次仅更新行业黑话词典，不修改其他业务项目的生产规则。

验证：

- `README.md`、`VERSION`和本更新日志的版本号一致为`v1.1`。
- 词典中不再存在`冷轧板`的“人工确认”标记。
- 未新增人名、公司名或具体业务主体信息。

## v1.0 — 2026-09-12

状态：首个公开基线。

更新内容：

- 建立板材行业同义词、常用简称、规格写法、加工术语、包装术语、报价表达、库存状态和业务状态词典。
- 收录冷轧类、冷硬类、冷成形类及边料、头尾板、回卷、包装方式等已确认映射。
- 补充整卷标识、厚度与宽度写法、价格增减表达、厚度“丝”、近似厚度、分条与平直加工、整卷采购和资源状态等术语。
- 建立“明确不能合并的近似词”和多义字符风险边界，避免将相近词错误归一。
- 建立公开脱敏规则：不包含真实人名、公司名及可识别具体业务主体的信息；不上传原始日志、截图、业务单据或内部记忆文件。
- 新增 `VERSION`、`CHANGELOG.md` 和仓库维护约定，作为后续版本维护基线。

验证：

- GitHub 默认分支中的词典正文与维护规则已回读。
- 已知敏感名称扫描无命中。
