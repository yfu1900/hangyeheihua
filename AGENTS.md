# AGENTS.md

## 仓库用途

- 本仓库只维护钢材贸易与加工验收领域的“行业黑话词典”。
- `README.md` 是词典的唯一正式版本，不在其他文件复制一份正文。
- 不与其他业务项目的代码、提示词、部署包或版本号混用。

## 维护规则

- 每次更新前先读取 GitHub 上最新的 `README.md`，避免覆盖其他人的修改。
- 保留黑话原文，同时提供标准词、适用范围、解释、置信度和风险边界。
- 能确定为同义词的内容放入“已确认或证据充分的同义词”。
- 只有上下文证据、尚未获得业务确认的内容标为“中”“条件性”或“待确认”，不得写成确定规则。
- 对容易误合并的词，必须同步维护“明确不能合并的近似词”。
- 规格、加工、包装、计价、库存和状态术语应分开归类；同一字符存在多义时必须记录作用域。
- 更新日志覆盖范围或出现次数时，必须实际重新核对数据，不沿用过期统计。
- 从内部记忆或日志提取知识时，只写消化后的通用行业知识，不复制原句或完整案例。

## 公开仓库安全边界

- 禁止上传原始聊天日志、截图、Excel业务单据、数据库导出或内部记忆文件。
- 禁止出现任何真实人名。
- 禁止出现任何真实公司名，包括客户、供应商、钢厂、加工厂、仓库经营方、平台和内部业务系统名称。
- 禁止写入订单号、货物编号、仓储号、真实价格明细、服务器地址、账号、密码、Token、Cookie、机器人密钥或数据库连接信息。
- 示例必须使用泛化或虚构数据；价格使用`N`等占位符，规格不得带有可识别具体订单的完整上下文。
- 来源描述只能写资料类型和时间范围，不得写发送人、业务主体、群名、仓库名或消息ID。
- 对可能暴露内部经营规则的内容，先提醒用户并取得明确公开授权。

## 版本管理

- 根目录 `VERSION` 是当前词典版本的唯一标识；当前基线为 `v1.0`。
- 每次修改前读取 `VERSION`、`CHANGELOG.md` 和最新 `README.md`。
- 用户确认并实际提交的一批词典修改只增加一次补丁版本，例如 `v1.0 → v1.1 → v1.2`。
- 仅查看、提取、分析、提出建议或等待确认时不增加版本。
- 每次版本更新必须同步修改 `VERSION`、`README.md` 顶部版本号和 `CHANGELOG.md`。
- `CHANGELOG.md` 的最新版本放在最前面，简要记录词条变化、边界变化和脱敏检查结果；历史记录不得删除或静默覆盖。

## 提交约定

- 小范围词典更新可直接提交到默认分支。
- 提交信息使用简洁中文，例如：`docs: 新增分条加工术语`。
- 每次提交后报告修改摘要、提交 SHA 和 GitHub 链接。
